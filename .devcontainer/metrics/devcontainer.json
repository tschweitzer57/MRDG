// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/ubuntu
{
	"name": "metrics",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	//	"image": "tschweitzer57/gtsam:ros2-humble", TODO: debuggen folgende Anweisung: odom_noise_model = gtsam.noiseModel.Diagonal.Sigmas([0.175, 0.175, 0.175, 0.05, 0.05, 0.05])
	"image": "tschweitzer57/gtsam:base-20.04",

	// Setup the workspace and mount on docker:
	"workspaceFolder": "/home/workspace",
	"mounts": [
		"source=${localWorkspaceFolder}/05_Metrics,target=/home/workspace,type=bind",
		"source=${localWorkspaceFolder}/06_Results/Computed,target=/home/workspace/input,type=bind",
		"source=${localWorkspaceFolder}/06_Results/Metrics,target=/home/workspace/saved_output,type=bind"
	]
	
	//"workspaceFolder": "/workspaces/",
	//"workspaceMount": "source=c:/Users/thiba/Documents/01_SyLRED/01_CARLoS/01_Tests/MESA/workspace,target=/home/mesa_workspace/wksp,type=bind"

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	// Use 'postCreateCommand' to run commands after the container is created.
	// "postCreateCommand": "uname -a",
	//
	// DISPLAY=192.168.14.255:0

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}